venv/
.venv/
__pycache__/
*.pyc
.streamlit/secrets.toml

# Extracted key frames (generated at runtime)
frames/

# Local environment / secrets — never commit real values
.env
.env.*
!.env.example

# Firebase service-account credentials — never commit real values
firebase-service-account.json
*.firebase-service-account.json
