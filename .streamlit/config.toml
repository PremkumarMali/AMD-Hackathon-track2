[theme]
# Values mirror components/theme.py — keep the two in sync.
base = "dark"
primaryColor = "#D4A15A"
backgroundColor = "#090806"
secondaryBackgroundColor = "#1B1713"
textColor = "#F5F2EC"
font = "sans serif"

[server]
maxUploadSize = 200
