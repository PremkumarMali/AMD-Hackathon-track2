[theme]
# Values mirror components/theme.py — keep the two in sync.
base = "dark"
primaryColor = "#d99e57"
backgroundColor = "#0e0b06"
secondaryBackgroundColor = "#17110a"
textColor = "#f1e9d7"
font = "sans serif"

[server]
maxUploadSize = 200
